---
import type { Locale } from "@/lib/i18n";

type Props = {
  locale: Locale;
  title?: string;
  description?: string;
};

const { locale, title = "Sam Lee", description = "Official website of Sam Lee." } = Astro.props;
---

<!doctype html>
<html lang={locale}>
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="description" content={description} />
    <meta name="theme-color" content="#080808" />
    
    <!-- OpenGraph SEO -->
    <meta property="og:type" content="website" />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:locale" content={locale === "zh-TW" ? "zh_TW" : locale === "zh-CN" ? "zh_CN" : "en_US"} />
    <meta property="og:site_name" content="Sam Lee" />
    
    <!-- Twitter Cards -->
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={title} />
    <meta name="twitter:description" content={description} />

    <title>{title}</title>
    <link rel="icon" href="/favicon.svg" />

    <!-- Google Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Chiron+Sung+HK:wght@200..900&family=Syncopate:wght@400;700&display=swap" rel="stylesheet" />
  </head>
  <body>
    <slot />
    <script>
      const nav = document.querySelector("[data-site-nav]");
      const menuButton = document.querySelector("[data-menu-button]");
      const mobileMenu = document.querySelector("[data-mobile-menu]");
      const updateNav = () => nav?.classList.toggle("is-scrolled", window.scrollY > 16);
      updateNav();
      window.addEventListener("scroll", updateNav, { passive: true });
      menuButton?.addEventListener("click", () => {
        const expanded = menuButton.getAttribute("aria-expanded") === "true";
        menuButton.setAttribute("aria-expanded", String(!expanded));
        mobileMenu?.classList.toggle("is-open", !expanded);
      });
      document.querySelectorAll("[data-scroll-tour]").forEach((link) => {
        link.addEventListener("click", (event) => {
          const target = document.querySelector("#tour");
          if (target) {
            event.preventDefault();
            mobileMenu?.classList.remove("is-open");
            menuButton?.setAttribute("aria-expanded", "false");
            target.scrollIntoView({ behavior: "smooth", block: "start" });
          }
        });
      });
    </script>
  </body>
</html>

<style is:global>
  :root {
    color-scheme: dark;
    --dark: #0F1418;
    --night-graphite: #151B20;
    --aqua-glow: #82E8E7;
    --aqua-glow-dark: #008080;
    --future-mist: #D8F4F3;
    --coral-red: #FF747D;
    --coral-red-dark: #710f28;
    --ice-white: #FAFAFA;

    --bg: var(--dark);
    --bg-soft: var(--night-graphite);
    --bg-muted: var(--night-graphite);
    --text: var(--ice-white);
    --text-muted: #9ca3af;
    --line: rgba(130, 232, 231, 0.2);
    --gold: var(--aqua-glow);
    --rose: var(--coral-red);
    --jade: var(--aqua-glow);
    --cyan: var(--aqua-glow);
    --pink: var(--coral-red);
    --grey: #2a2a30;
    --max: 1160px;

    /* Memorized current font selection in case we need to revert:
    font-family:
      Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI",
      "Noto Sans TC", "Noto Sans SC", sans-serif;
    */

    /* Default font selection (Chinese/All text uses Chiron Sung HK by default) */
    font-family:
      "Chiron Sung HK", Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI",
      "Noto Sans TC", "Noto Sans SC", sans-serif;
  }

  /* English-specific body/normal text */
  html[lang="en"] {
    font-family:
      Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI",
      sans-serif;
  }

  /* English titles override: Syncopate for news, video, album, and other section/page titles */
  html[lang="en"] .section-title,
  html[lang="en"] .page-title,
  html[lang="en"] .news-text h2,
  html[lang="en"] .news-text .eyebrow {
    font-family: "Syncopate", sans-serif;
  }

  * {
    box-sizing: border-box;
  }

  html {
    background: var(--bg);
    scroll-behavior: smooth;
  }

  body {
    margin: 0;
    min-height: 100vh;
    background: var(--bg);
    color: var(--text);
  }

  a {
    color: inherit;
    text-decoration: none;
  }

  img,
  video,
  iframe {
    max-width: 100%;
  }

  button,
  input,
  textarea,
  select {
    font: inherit;
  }

  .container {
    width: min(100% - 32px, var(--max));
    margin-inline: auto;
  }

  .section {
    padding: 72px 0;
  }

  .section-title {
    margin: 0 0 24px;
    font-size: clamp(2rem, 5vw, 4.4rem);
    line-height: 0.95;
    font-weight: normal;
    letter-spacing: -0.02em;
    text-transform: uppercase;
    color: var(--aqua-glow);
    text-shadow: 0 0 8px rgba(130, 232, 231, 0.4), 0 0 20px rgba(130, 232, 231, 0.1);
    position: relative;
    display: block;
  }

  .section-title::after {
    content: "";
    display: block;
    width: 35%;
    height: 4px;
    background: var(--aqua-glow);
    margin-top: 8px;
    box-shadow: 0 -4px 10px var(--aqua-glow);
  }

  .muted {
    color: var(--text-muted);
  }

  .empty-state {
    border: 1px solid var(--line);
    background: linear-gradient(135deg, rgba(255, 255, 255, 0.06), rgba(255, 255, 255, 0.02));
    padding: 32px;
    min-height: 160px;
    display: grid;
    place-items: center;
    text-align: center;
    color: var(--text-muted);
  }

  .external-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(180px, 1fr));
    gap: 16px;
  }

  .platform-link {
    min-height: 132px;
    border: 1px solid var(--line);
    background: #101010;
    display: grid;
    place-items: center;
    gap: 12px;
    padding: 18px;
    transition:
      transform 180ms ease,
      border-color 180ms ease,
      background 180ms ease;
  }

  .platform-link:hover {
    transform: translateY(-3px);
    border-color: var(--cyan);
    background: #081112;
    box-shadow: 0 0 12px rgba(75, 187, 196, 0.3);
  }

  .platform-link img {
    width: 96px;
    height: 48px;
    object-fit: contain;
  }

  .button {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    min-height: 44px;
    padding: 0 22px;
    border: 2px solid var(--coral-red);
    background: var(--night-graphite);
    color: var(--ice-white);
    font-weight: 800;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    box-shadow: 0 0 8px rgba(255, 116, 125, 0.2);
    transition:
      transform 180ms ease,
      background 180ms ease,
      color 180ms ease,
      box-shadow 180ms ease;
  }

  .button:hover {
    transform: translateY(-2px);
    background: rgba(255, 116, 125, 0.6);
    color: var(--ice-white);
    box-shadow: 0 0 16px rgba(255, 116, 125, 0.4);
  }

  @media (max-width: 720px) {
    .section {
      padding: 52px 0;
    }

    .container {
      width: min(100% - 24px, var(--max));
    }
  }
</style>
